# Women Safety (Expo)

This is a simple Expo prototype demonstrating: login/signup, trusted contacts, hold-to-SOS (3s), location sharing, fake call, and safety tips.

Run:

```bash
npm install
npx expo start -c
```
